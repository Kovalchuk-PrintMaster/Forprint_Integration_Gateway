# Gateway v0.6 Consumer Acceptance Fixtures

Gateway v0.6 adds consumer acceptance fixtures for every supported consumer
bundle.

Covered consumers:

```text
forprint_crm
forprint_operational_registry
calculator_engine
forprint_prepress_hub
forprint_accounting_registry
telegram_bot
website
mobile_app

Each fixture declares expected contract version, compatibility state, dry-run
plan status, required fields, forbidden fields, live-enabled false and expected
boundary status.


```text